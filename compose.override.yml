# development overrides for compose.yml
services:
  bot:
    build: .
    environment:
      ENVIRONMENT: dev
      HEALTH_CHECK_URL: "http://localhost:${API_PORT:-6502}${API_ROOT_PATH:-}/health"

  postgres:
    ports:
      - 6543:5432

secrets:
  env:
    file: .env
