# production overrides for compose.yml
services:
  bot:
    restart: unless-stopped
    image: "${IMAGE}"
    environment:
      HEALTH_CHECK_URL: "${API_BASE_URL}${API_ROOT_PATH}/health"

  postgres:
    restart: unless-stopped

secrets:
  env:
    file: .env
